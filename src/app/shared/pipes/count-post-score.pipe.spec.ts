import { CountPostScorePipe } from './count-post-score.pipe';

describe('CountPostScorePipe', () => {
  it('create an instance', () => {
    const pipe = new CountPostScorePipe();
    expect(pipe).toBeTruthy();
  });
});
