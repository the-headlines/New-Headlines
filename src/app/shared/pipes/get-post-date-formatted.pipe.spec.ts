import { GetPostDateFormattedPipe } from './get-post-date-formatted.pipe';

describe('GetPostDateFormattedPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPostDateFormattedPipe();
    expect(pipe).toBeTruthy();
  });
});
